apiVersion: v1
kind: Service
metadata:
  name: service-1pod-{{.Iteration}}-{{.Replica}}
  labels:
    app: deployment-1pod-{{.Iteration}}-{{.Replica}}
spec:
  selector:
    app: deployment-1pod-{{.Iteration}}-{{.Replica}}
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP